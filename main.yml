---
- hosts: all
  vars:
    user: watanabe_shingo
    rbenv_user: "{{user}}"
    nvm_user: "{{user}}"
    pyenv_user: "{{user}}"
  roles:
    - kernel-devel
    - zsh
    - user
    - vim
    - tmux
    - ag
    - ctags
    - nvm
    - mysql
    - rbenv
    - pyenv
