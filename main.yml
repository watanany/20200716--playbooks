---
- hosts: all
  vars:
    user: vagrant
    rbenv_user: "{{user}}"
    nvm_user: "{{user}}"
    pyenv_user: "{{user}}"
  roles:
    - ag
    - ctags
    - tmux
    - zsh
    - vim
    - mysql
    - user
    - pyenv
    - rbenv
    - nvm
